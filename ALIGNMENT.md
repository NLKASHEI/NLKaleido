# v0.10.0 交接需求对齐表

本表对照《万花筒交接稿.md》与 2026-08-15 的产品修订要求。状态分为“自动验收通过”和“需真实酒馆人工点验”，避免把尚未实机点击的内容写成已验证。

## 已实现并有自动测试

| 需求 | v0.10.0 行为 | 验收依据 |
|---|---|---|
| 传统 MVU 单次更新 | 极简档从配置钳制与运行时两层硬锁为一次请求 | `config.test.ts`、`adapter.test.ts`、`integration.test.ts` |
| 极简档不运行记忆和世界推演 | 即使角色契约已开启，极简档也不注入记忆、不写记忆候选、不运行推演、不注册记忆工具 | `integration.test.ts` 与适配器门控 |
| 普通档可选择单次或多步 | 普通/进阶档允许 1～8 次串行 Agent；相邻请求可设间隔，避免撞 RPM | 配置钳制测试、Agent 工具循环测试 |
| 单次模式的记忆写入 | 普通/进阶档启用记忆后，反思候选合并进同一次变量请求；候选会校验、写入、落盘并触发事件 | `integration.test.ts`“反射字段合并进同一变量请求” |
| 纯记忆插件 | 契约允许 0 个变量；反思轮仍会请求、写入、计轮和落盘，非反思轮零模型请求 | `integration.test.ts`“纯记忆模式” |
| 记忆可观察性 | 浮动入口显示运行阶段；记忆页显示整理周期、下次轮次、归档进度和持久化回执；新增、强化、拒绝、失败均可核对 | 适配层回执模型、面板事件与集成测试 |
| 纪要事实约束 | 长期记忆只收明确发生的原子事实；归档保留轮次、前后值和明确因果，禁止推测、情绪化解读与总结升华 | `memory.test.ts` 候选审计、归档提示词 |
| AI 不得偷懒漏审变量 | 到期变量必须逐项给出更新/不变与依据；遗漏或结论冲突会拒绝并自纠 | `agent.test.ts`、集成测试 |
| 作者零代码构造 | 变量、默认值、更新规则、玩家显示、世界书联动、检定和可选系统均有表单 | 作者模型与契约保存测试 |
| 分板块 AI 帮帮 | 变量设计、世界书联动、检定、可选系统、ZOD 迁移各有独立问题表、后台提示和写入白名单 | `authorSkill.test.ts` |
| 世界书读取 | 读取当前角色主世界书、聊天世界书、卡内嵌世界书及具体条目；联动只引用真实条目 | `adapter.test.ts`“作者端当前角色世界书浏览” |
| 独立 API 模型列表 | URL/Key 经酒馆同源后端代理请求 `/models`；返回后用选择框选模型，手填是异常兜底 | `adapter.test.ts`“独立主 AI 端点” |
| 前端变量读写 | 已声明变量可 `get/set/setMany/list/subscribe`；类型、约束、审计、原子落盘均生效 | `bridge.test.ts`；`FRONTEND_API.md` |
| MVU/ZOD 迁移 | 本地确定性检测、ZOD 解析、规则提取、人工确认；AI 仅审校，不得删除或猜测 | `migration.test.ts`、`authorSkill.test.ts` |
| 可选模块分包 | memory/config/migration 为动态 chunk，主入口不包含其实现签名 | 构建后的 `verify-release.mjs` |

## 交互重构

- 首次进入先选择玩家或作者；玩家默认看“变量概览”，作者进入构造工作台。
- 运行档位默认展开并使用直白名称：“极简 · 仅变量 / 普通 · 推荐 / 进阶 · 向量”。
- 模型配置改为“URL + Key → 连接并读取模型 → 下拉选择”，不再把模型名输入框当主流程。
- AI 帮帮不再共用一个泛化输入框；每个板块分别询问目标、依据和硬约束。
- 记忆页参考酒馆 Data Bank 的信息组织：统计、条目/表格分页、搜索筛选、作用域分组、状态与保留度、行级操作。
- 记忆页新增整理倒计时、归档门槛进度与折叠回执；后台 JSON/纪要自动隐藏，不插入聊天正文。
- 作者可直接配置“每几轮整理、多少修改后归档、批大小、最大记忆数”，0 变量时明确标为“纯记忆模式”。
- 所有主题文字都使用工作台自己的颜色变量；禁用酒馆主题对按钮悬停附加的变暗滤镜。

## 产品口径修订

交接稿 §20.13.4 的旧表曾把极简档描述为可带原子记忆/BM25；2026-08-15 的明确产品要求改为：

- 极简档只做一次变量更新，记忆、世界推演和工具循环全部暂停；
- 普通档开始允许作者按契约开启记忆和世界推演；
- 进阶档在普通档之上增加可选向量检索。

v0.10.0 以最新产品要求为准，并在 UI、配置保存和运行时三层保持一致。

## 仍需真实酒馆人工点验

- 在目标酒馆版本中安装 v0.10.0 后，逐项点击亮色/深色/纸张主题、模型下拉、作者各板块 AI 草稿确认、世界书条目选择和记忆列表操作。
- 使用真实 OpenAI-compatible 服务验证 `/models` 返回差异、模型生成与向量接口；自动测试覆盖代理 URL、鉴权头和响应解析，但不会消耗真实供应商额度。
- 长篇角色卡进行 20 轮以上回归，观察真实提示词质量、记忆反思内容和世界推演节奏。

这些人工项不影响构建与自动测试结论，但在完成前不宣称“所有供应商、所有酒馆主题均已实机无差异”。
